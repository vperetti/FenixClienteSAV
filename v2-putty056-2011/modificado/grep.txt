File BE_ALL.C:
 * This appname is not strictly in the right place, since Plink
 * of the dialog-box sorts of things that make use of appname, so
 * declarations of appname, for as long as I can...
const char *const appname = "PuTTY";
File BE_NOSSH.C:
const char *const appname = "PuTTYtel";
File CONFIG.C:
    str = dupprintf("Basic options for your %s session", appname);
    str = dupprintf("Options controlling %s's window", appname);
    str = dupprintf("Configure the appearance of %s's window", appname);
    str = dupprintf("Configure the behaviour of %s's window", appname);
    str = dupprintf("Adjust how %s handles line drawing characters", appname);
    str = dupprintf("Adjust the precise colours %s displays", appname);
File CONFIG.C~:
    str = dupprintf("Basic options for your %s session", appname);
    str = dupprintf("Options controlling %s's window", appname);
    str = dupprintf("Configure the appearance of %s's window", appname);
    str = dupprintf("Configure the behaviour of %s's window", appname);
    str = dupprintf("Adjust how %s handles line drawing characters", appname);
    str = dupprintf("Adjust the precise colours %s displays", appname);
pageant.tds: grep: input lines truncated - result questionable
plink.tds: grep: input lines truncated - result questionable
pscp.tds: grep: input lines truncated - result questionable
psftp.tds: grep: input lines truncated - result questionable
File PUTTY.H:
extern const char *const appname;
putty.tds: grep: input lines truncated - result questionable
puttygen.tds: grep: input lines truncated - result questionable
puttytel.tds: grep: input lines truncated - result questionable
File WINCFG.C:
    str = dupprintf("Adjust how %s displays line drawing characters", appname);
File WINCFG.C~:
    str = dupprintf("Adjust how %s displays line drawing characters", appname);
File WINDLG.C:
	    char *str = dupprintf("%s Event Log", appname);
	    char *str = dupprintf("%s Licence", appname);
	str = dupprintf("Diretórios %s", appname);
	SetDlgItemText(hwnd, IDA_TEXT1, appname);
    dp.wintitle = dupprintf("%s Configuration", appname);
    dp.errtitle = dupprintf("%s Error", appname);
    dp.wintitle = dupprintf("Opções", appname);
    dp.errtitle = dupprintf("%s Erro", appname);
	message = dupprintf(wrongmsg, appname, keytype, fingerprint, appname);
	title = dupprintf(mbtitle, appname);
	message = dupprintf(absentmsg, keytype, fingerprint, appname);
	title = dupprintf(mbtitle, appname);
    title = dupprintf(mbtitle, appname);
    mbtitle = dupprintf("%s Log to File", appname);
    msg = dupprintf(message, appname);
    title = dupprintf(mbtitle, appname);
File WINDLG.C~:
	    char *str = dupprintf("%s Event Log", appname);
	    char *str = dupprintf("%s Licence", appname);
	str = dupprintf("Diretórios %s", appname);
	SetDlgItemText(hwnd, IDA_TEXT1, appname);
    dp.wintitle = dupprintf("%s Configuration", appname);
    dp.errtitle = dupprintf("%s Error", appname);
    dp.wintitle = dupprintf("Opções", appname);
    dp.errtitle = dupprintf("%s Erro", appname);
	message = dupprintf(wrongmsg, appname, keytype, fingerprint, appname);
	title = dupprintf(mbtitle, appname);
	message = dupprintf(absentmsg, keytype, fingerprint, appname);
	title = dupprintf(mbtitle, appname);
    title = dupprintf(mbtitle, appname);
    mbtitle = dupprintf("%s Log to File", appname);
    msg = dupprintf(message, appname);
    title = dupprintf(mbtitle, appname);
File WINDOW.C:
	char *str = dupprintf("%s Fatal Error", appname);
				   "to continue?", appname);
		    s2 = dupprintf("%s Warning", appname);
	    char *str = dupprintf("%s Internal Error", appname);
	char *str = dupprintf("%s Internal Error", appname);
	wndclass.lpszClassName = appname;
	hwnd = CreateWindowEx(exwinmode, appname, appname,
	    char *str = dupprintf("%s Error", appname);
	    sprintf(msg, "%s - %s", realhost, appname);
	    str = dupprintf("&Diretórios %s", appname);
    sprintf(morestuff, "%.70s Fatal Error", appname);
	sprintf(morestuff, "%.70s (inactive)", appname);
    sprintf(morestuff, "%.70s Command Line Error", appname);
	    sprintf(morestuff, "%.70s (inactive)", appname);
		       appname, MB_OK | MB_ICONINFORMATION);
	    str = dupprintf("Confirmação de saída", appname);
    sprintf(morestuff, "%.70s Fatal Error", appname);
    sprintf(morestuff, "%.70s Fatal Error", appname);
	    sprintf(otherbuf, "%.70s Sound Error", appname);
File WINDOW.C~:
	char *str = dupprintf("%s Fatal Error", appname);
				   "to continue?", appname);
		    s2 = dupprintf("%s Warning", appname);
	    char *str = dupprintf("%s Internal Error", appname);
	char *str = dupprintf("%s Internal Error", appname);
	wndclass.lpszClassName = appname;
	hwnd = CreateWindowEx(exwinmode, appname, appname,
	    char *str = dupprintf("%s Error", appname);
	    sprintf(msg, "%s - %s", realhost, appname);
	    str = dupprintf("&About %s", appname);
    sprintf(morestuff, "%.70s Fatal Error", appname);
	sprintf(morestuff, "%.70s (inactive)", appname);
    sprintf(morestuff, "%.70s Command Line Error", appname);
	    sprintf(morestuff, "%.70s (inactive)", appname);
		       appname, MB_OK | MB_ICONINFORMATION);
	    str = dupprintf("Confirmação de saída", appname);
    sprintf(morestuff, "%.70s Fatal Error", appname);
    sprintf(morestuff, "%.70s Fatal Error", appname);
	    sprintf(otherbuf, "%.70s Sound Error", appname);
WINDOW.obj: grep: input lines truncated - result questionable
